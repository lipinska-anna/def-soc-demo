import { ConvertMarkdownPipe } from './convert-markdown.pipe';

describe('ConvertMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
